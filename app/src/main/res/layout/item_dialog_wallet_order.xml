<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="rechargeActivityBean"
            type="com.tx.zygj.bean.WalletRechargeBean.RechargeActivityBean" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:background="@color/white">

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:text="@{rechargeActivityBean.activityName}"
            android:textColor="@color/color_3D4255"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/activity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_toEndOf="@id/textView"
            android:text="（不满足最低金额无法参加活动)"
            android:textSize="12sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/choice"
            android:layout_width="40dp"
            android:layout_height="25dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="20dp"
            android:gravity="center"
            android:textColor="@color/white"
            android:background="@drawable/change_fc664a_f38b26_4"
            android:text="选择" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/textView"
            android:layout_marginStart="10dp"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="10dp"
            android:text="@{`充值满` + rechargeActivityBean.rechargeAmount + `元.赠送` + rechargeActivityBean.give + `元、赠送积分` + rechargeActivityBean.giveIntegral}"
            android:textSize="12sp" />
    </RelativeLayout>
</layout>